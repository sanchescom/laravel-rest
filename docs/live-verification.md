# Live Verification

Generated 2026-09-16 04:29 UTC from `1.6.0-34-g771ed8e` by `composer live:catalog && composer live:report`. Do not edit by hand.

62 APIs, 470 scenarios: ✅ 365 passed · ❌ 0 failed · ⏭ 1 skipped (API unavailable) · 🚫 104 unsupported · ⬜ 0 not run.

## Feature coverage

A feature counts as confirmed on an API when at least one of its scenarios passed there. Fewer than 3 APIs is marked ⚠️.

| Feature | Description | Confirmed on | ✅ APIs | ❌ | ⏭ | 🚫 |
| --- | --- | --- | --- | --- | --- | --- |
| `read.list` | List a collection | 47 | Art Institute of Chicago API, AviationWeather.gov Data API, Bluesky AppView (XRPC), Câmara dos Deputados Dados Abertos, Cat Facts, City of Chicago (Socrata SODA), CKAN Action API (demo.ckan.org), crates.io, Crossref REST API, Disney API, Drupal.org JSON:API, DummyJSON, Europe PMC REST, Fake Store API, FBI Wanted, U.S. Treasury Fiscal Data, GBIF API, GOV.UK Bank Holidays, IBGE Localidades, JSONPlaceholder, mail.tm, MyGene.info, NHTSA vPIC, Nobel Prize API 2.1, npm Registry, OData Northwind v4, Open5e, OpenAlex, Open Brewery DB, OpenF1, openFDA, Open Library Search, OpenVan.camp, PokéAPI, Postcodes.io, PotterDB, Radio Browser, Represent (Open North), ReqRes, restful-api.dev, Rick and Morty API, Spaceflight News API, data.police.uk, USGS Earthquake Catalog (FDSN), Wikipedia Action API, Quotes on Design (WordPress REST API), World Bank Indicators API |  |  | AviationWeather.gov Data API, Bank of Russia daily rates (XML), Dog CEO, Genrenator, Countries GraphQL API, Hacker News (Firebase), Open-Meteo, OpenSky Network, Postman Echo, Solana JSON-RPC (mainnet-beta), USGS Earthquake Catalog (FDSN) |
| `read.data-key` | Read items under a data key | 37 | Art Institute of Chicago API, Bluesky AppView (XRPC), Câmara dos Deputados Dados Abertos, Cat Facts, CKAN Action API (demo.ckan.org), crates.io, Crossref REST API, Disney API, Drupal.org JSON:API, DummyJSON, Europe PMC REST, FBI Wanted, U.S. Treasury Fiscal Data, GBIF API, GOV.UK Bank Holidays, Countries GraphQL API, mail.tm, MyGene.info, NHTSA vPIC, Nobel Prize API 2.1, npm Registry, OData Northwind v4, Open5e, OpenAlex, openFDA, Open Library Search, OpenVan.camp, PokéAPI, Postcodes.io, PotterDB, Represent (Open North), ReqRes, Rick and Morty API, Spaceflight News API, USGS Earthquake Catalog (FDSN), Wikipedia Action API, World Bank Indicators API |  |  |  |
| `read.data-key.nested` | Read items under a nested (dot) data key | 7 | CKAN Action API (demo.ckan.org), Crossref REST API, Europe PMC REST, GOV.UK Bank Holidays, Countries GraphQL API, httpbin, Wikipedia Action API |  |  |  |
| `read.find` | Find one model by key | 32 | Art Institute of Chicago API, Câmara dos Deputados Dados Abertos, City of Chicago (Socrata SODA), crates.io, Crossref REST API, Disney API, Drupal.org JSON:API, DummyJSON, Fake Store API, FBI Wanted, GBIF API, GitHub REST API (anonymous), IBGE Localidades, icanhazdadjoke, JSONPlaceholder, MyGene.info, Nobel Prize API 2.1, npm Registry, Open5e, Open Brewery DB, OpenVan.camp, PokéAPI, Postcodes.io, PotterDB, ReqRes, restful-api.dev, Rick and Morty API, Scryfall, Spaceflight News API, data.police.uk, Quotes on Design (WordPress REST API), World Bank Indicators API |  |  | AviationWeather.gov Data API, Cat Facts, CKAN Action API (demo.ckan.org), U.S. Treasury Fiscal Data, Genrenator, Countries GraphQL API, Hacker News (Firebase), NHTSA vPIC, OData Northwind v4, OpenAlex, OpenF1, openFDA, Open Library Search, OpenSky Network, Postcodes.io, Represent (Open North), Solana JSON-RPC (mainnet-beta), Wikipedia Action API |
| `read.get-many` | Fetch several models concurrently (getMany) | 23 | Art Institute of Chicago API, Câmara dos Deputados Dados Abertos, Crossref REST API, Disney API, DummyJSON, Fake Store API, GBIF API, IBGE Localidades, JSONPlaceholder, MyGene.info, Nobel Prize API 2.1, npm Registry, Open5e, Open Brewery DB, PokéAPI, Postcodes.io, PotterDB, ReqRes, restful-api.dev, Rick and Morty API, Scryfall, Spaceflight News API, data.police.uk |  |  | OpenAlex, Represent (Open North) |
| `query.filter` | Filter on the server (where) | 18 | Câmara dos Deputados Dados Abertos, City of Chicago (Socrata SODA), CKAN Action API (demo.ckan.org), Drupal.org JSON:API, FBI Wanted, GBIF API, JSONPlaceholder, Nobel Prize API 2.1, OData Northwind v4, Open5e, Open Brewery DB, OpenF1, PotterDB, Represent (Open North), Rick and Morty API, Spaceflight News API, data.police.uk, Quotes on Design (WordPress REST API) |  |  | Art Institute of Chicago API, CKAN Action API (demo.ckan.org), Crossref REST API, Disney API, DummyJSON, U.S. Treasury Fiscal Data, Countries GraphQL API, MyGene.info, OpenAlex, OpenF1, openFDA, OpenSky Network, PokéAPI, Postman Echo, ReqRes, restful-api.dev, Scryfall, Spaceflight News API |
| `query.where-in` | Membership filter (whereIn) | 9 | Agify.io, Art Institute of Chicago API, AviationWeather.gov Data API, Open5e, Open Brewery DB, PotterDB, Radio Browser, restful-api.dev, Spaceflight News API |  | Câmara dos Deputados Dados Abertos | crates.io, DummyJSON, U.S. Treasury Fiscal Data, GBIF API, IBGE Localidades, JSONPlaceholder, Open5e, OpenAlex, OpenF1, Postcodes.io, Represent (Open North), ReqRes, Rick and Morty API |
| `query.sort` | Sort on the server (orderBy) | 18 | Câmara dos Deputados Dados Abertos, City of Chicago (Socrata SODA), CKAN Action API (demo.ckan.org), Crossref REST API, Drupal.org JSON:API, DummyJSON, FBI Wanted, U.S. Treasury Fiscal Data, JSONPlaceholder, OData Northwind v4, Open5e, OpenAlex, Open Brewery DB, openFDA, PotterDB, Scryfall, Spaceflight News API, Quotes on Design (WordPress REST API) |  |  | Art Institute of Chicago API, crates.io, Fake Store API, GBIF API, IBGE Localidades, Nobel Prize API 2.1, Open Library Search, PokéAPI, Radio Browser, ReqRes, restful-api.dev, Rick and Morty API, data.police.uk, USGS Earthquake Catalog (FDSN) |
| `grammar.plain` | Plain grammar | 12 | Art Institute of Chicago API, AviationWeather.gov Data API, Cat Facts, GBIF API, Nobel Prize API 2.1, OpenF1, Open Library Search, PokéAPI, Radio Browser, restful-api.dev, Rick and Morty API, data.police.uk |  |  |  |
| `grammar.jsonapi` | JSON:API grammar | 3 | Drupal.org JSON:API, U.S. Treasury Fiscal Data, PotterDB |  |  |  |
| `grammar.django` | Django grammar | 3 | Open5e, Represent (Open North), Spaceflight News API |  |  |  |
| `grammar.configurable` | Configurable grammar (names, sort styles, casing) | 24 | Agify.io, Câmara dos Deputados Dados Abertos, CKAN Action API (demo.ckan.org), crates.io, Crossref REST API, Disney API, Drupal.org JSON:API, DummyJSON, FBI Wanted, GitHub REST API (anonymous), JSONPlaceholder, MyGene.info, npm Registry, OData Northwind v4, Open5e, OpenAlex, Open Brewery DB, openFDA, Represent (Open North), ReqRes, Scryfall, Spaceflight News API, Quotes on Design (WordPress REST API), World Bank Indicators API |  | Câmara dos Deputados Dados Abertos |  |
| `grammar.custom` | Custom Grammar class | 3 | City of Chicago (Socrata SODA), CKAN Action API (demo.ckan.org), OData Northwind v4 |  |  |  |
| `paginate.total` | paginate() with a response total | 25 | Art Institute of Chicago API, Cat Facts, CKAN Action API (demo.ckan.org), crates.io, Crossref REST API, DummyJSON, FBI Wanted, U.S. Treasury Fiscal Data, GBIF API, MyGene.info, Nobel Prize API 2.1, npm Registry, OData Northwind v4, Open5e, OpenAlex, openFDA, Open Library Search, PokéAPI, PotterDB, Represent (Open North), ReqRes, Rick and Morty API, Scryfall, Spaceflight News API, World Bank Indicators API |  |  | Bluesky AppView (XRPC), Câmara dos Deputados Dados Abertos, Cat Facts, City of Chicago (Socrata SODA), Disney API, Drupal.org JSON:API, Europe PMC REST, Fake Store API, GitHub REST API (anonymous), JSONPlaceholder, mail.tm, Open Brewery DB, Postman Echo, Radio Browser, restful-api.dev, Quotes on Design (WordPress REST API) |
| `paginate.simple` | simplePaginate() | 21 | Art Institute of Chicago API, Câmara dos Deputados Dados Abertos, Cat Facts, City of Chicago (Socrata SODA), Disney API, Drupal.org JSON:API, DummyJSON, U.S. Treasury Fiscal Data, GitHub REST API (anonymous), JSONPlaceholder, Nobel Prize API 2.1, Open5e, Open Brewery DB, PokéAPI, PotterDB, Represent (Open North), ReqRes, Rick and Morty API, Scryfall, Spaceflight News API, Quotes on Design (WordPress REST API) |  |  | Bluesky AppView (XRPC), Fake Store API, icanhazdadjoke, mail.tm, restful-api.dev, USGS Earthquake Catalog (FDSN), Wikipedia Action API |
| `paginate.lazy` | lazy() walk across pages | 25 | Art Institute of Chicago API, Câmara dos Deputados Dados Abertos, Cat Facts, City of Chicago (Socrata SODA), Crossref REST API, Disney API, DummyJSON, FBI Wanted, U.S. Treasury Fiscal Data, GBIF API, JSONPlaceholder, MyGene.info, Nobel Prize API 2.1, Open5e, Open Brewery DB, Open Library Search, PokéAPI, PotterDB, Radio Browser, Represent (Open North), ReqRes, Rick and Morty API, Spaceflight News API, Quotes on Design (WordPress REST API), World Bank Indicators API |  |  | AviationWeather.gov Data API, Bluesky AppView (XRPC), Drupal.org JSON:API, Europe PMC REST, Fake Store API, npm Registry, OpenAlex, openFDA, restful-api.dev, Scryfall, data.police.uk, USGS Earthquake Catalog (FDSN), Wikipedia Action API, Quotes on Design (WordPress REST API) |
| `paginate.style.page` | Page-number pagination style | 13 | Art Institute of Chicago API, Cat Facts, crates.io, FBI Wanted, U.S. Treasury Fiscal Data, JSONPlaceholder, Open5e, OpenAlex, PotterDB, ReqRes, Rick and Morty API, Scryfall, World Bank Indicators API |  |  |  |
| `paginate.style.offset` | Offset pagination style | 14 | CKAN Action API (demo.ckan.org), Crossref REST API, DummyJSON, GBIF API, MyGene.info, Nobel Prize API 2.1, npm Registry, OData Northwind v4, openFDA, Open Library Search, PokéAPI, Radio Browser, Represent (Open North), Spaceflight News API |  |  |  |
| `relation.belongs-to` | belongsTo | 7 | Art Institute of Chicago API, Câmara dos Deputados Dados Abertos, DummyJSON, Fake Store API, GBIF API, JSONPlaceholder, Quotes on Design (WordPress REST API) |  |  | Open5e, OpenAlex, OpenF1, Open Library Search, PotterDB, ReqRes, Rick and Morty API, Spaceflight News API |
| `relation.has-many` | hasMany by foreign key | 3 | Câmara dos Deputados Dados Abertos, GBIF API, JSONPlaceholder |  |  | DummyJSON, Hacker News (Firebase), Open5e, OpenF1, ReqRes, Rick and Morty API, Spaceflight News API |
| `relation.nested` | Nested URL relation | 9 | Câmara dos Deputados Dados Abertos, crates.io, Crossref REST API, DummyJSON, GBIF API, IBGE Localidades, JSONPlaceholder, PotterDB, data.police.uk |  |  | Dog CEO, NHTSA vPIC, ReqRes |
| `eager.concurrent` | Eager loading, concurrent | 9 | Art Institute of Chicago API, Câmara dos Deputados Dados Abertos, DummyJSON, Fake Store API, GBIF API, JSONPlaceholder, Open5e, OpenF1, PotterDB |  |  | data.police.uk |
| `eager.batch` | Eager loading, batched whereIn | 3 | Câmara dos Deputados Dados Abertos, Open5e, restful-api.dev |  |  | Art Institute of Chicago API, DummyJSON, GBIF API, JSONPlaceholder, OpenF1, PotterDB |
| `write.create` | Create (POST) | 8 | DummyJSON, Fake Store API, httpbin, httpbingo, JSONPlaceholder, Postman Echo, ReqRes, restful-api.dev |  |  | CKAN Action API (demo.ckan.org), mail.tm |
| `write.update` | Update (PUT) | 7 | DummyJSON, Fake Store API, httpbin, JSONPlaceholder, Postman Echo, ReqRes, restful-api.dev |  |  |  |
| `write.patch` | Update (PATCH) | 7 | DummyJSON, Fake Store API, httpbin, JSONPlaceholder, Postman Echo, ReqRes, restful-api.dev |  |  |  |
| `write.delete` | Delete | 7 | DummyJSON, Fake Store API, httpbin, JSONPlaceholder, Postman Echo, ReqRes, restful-api.dev |  |  |  |
| `write.envelope` | Request data envelope (requestDataKey) | 3 | httpbin, Postman Echo, ReqRes |  |  |  |
| `errors.not-found` | 404 → ModelNotFoundException | 28 | Art Institute of Chicago API, Câmara dos Deputados Dados Abertos, crates.io, Crossref REST API, Dog CEO, DummyJSON, FBI Wanted, GBIF API, GitHub REST API (anonymous), httpbin, httpbingo, JSONPlaceholder, MyGene.info, npm Registry, Open5e, Open Brewery DB, OpenVan.camp, PokéAPI, Postcodes.io, Postman Echo, PotterDB, ReqRes, restful-api.dev, Rick and Morty API, Scryfall, Spaceflight News API, data.police.uk, Quotes on Design (WordPress REST API) |  |  | CKAN Action API (demo.ckan.org), Disney API, Drupal.org JSON:API, Fake Store API, Countries GraphQL API, Hacker News (Firebase), IBGE Localidades, icanhazdadjoke, NHTSA vPIC, Nobel Prize API 2.1, OpenF1, openFDA, Represent (Open North), Wikipedia Action API, World Bank Indicators API |
| `errors.validation` | 422 → ValidationException | 6 | DummyJSON, GitHub REST API (anonymous), httpbin, httpbingo, OpenVan.camp, Postman Echo |  |  |  |
| `errors.client` | Other 4xx → RequestException | 13 | Câmara dos Deputados Dados Abertos, Crossref REST API, DummyJSON, U.S. Treasury Fiscal Data, httpbin, httpbingo, Open-Meteo, Postman Echo, ReqRes, restful-api.dev, Scryfall, Spaceflight News API, USGS Earthquake Catalog (FDSN) |  |  |  |
| `errors.server` | 5xx → ServerException | 4 | DummyJSON, httpbin, httpbingo, Postman Echo |  |  |  |
| `auth.bearer` | Bearer auth | 4 | DummyJSON, httpbin, httpbingo, Postman Echo |  |  |  |
| `auth.basic` | Basic auth | 3 | httpbin, httpbingo, Postman Echo |  |  |  |
| `auth.header` | Header auth | 3 | httpbin, Postman Echo, ReqRes |  |  |  |
| `headers.dynamic` | Dynamic headers (withHeaders) | 5 | httpbin, httpbingo, icanhazdadjoke, Postman Echo, Scryfall |  |  |  |
| `retry.status` | Retry on retryable status | 4 | DummyJSON, httpbin, httpbingo, Postman Echo |  |  | Câmara dos Deputados Dados Abertos |
| `cache.response` | Response cache | 6 | Art Institute of Chicago API, Cat Facts, httpbin, JSONPlaceholder, Postman Echo, ReqRes |  |  |  |
| `cache.memo` | Request memoization | 5 | DummyJSON, JSONPlaceholder, Rick and Morty API, Spaceflight News API, data.police.uk |  |  |  |

## APIs

| API | Base URI | Structure | ✅ | ❌ | ⏭ | 🚫 |
| --- | --- | --- | --- | --- | --- | --- |
| Agify.io | https://api.agify.io/ | response: bare-object (single) / bare-array (name[] batch)<br>pagination: none<br>filters: name= / name[]=<br>rate_limit: documented 100 names/day anonymous — one scenario only | 1 | 0 | 0 | 0 |
| Art Institute of Chicago API | https://api.artic.edu/api/v1/ | response: data envelope (list and detail) + pagination/info/config siblings<br>pagination: page + limit, total/next_url under pagination.*<br>filters: ids=a,b only on GET; field filters and sort exist only on /artworks/search (ES DSL)<br>keys: int id<br>relations: fk artist_id -> agents/{id}<br>errors: 404 {"status","error","detail"} | 11 | 0 | 0 | 2 |
| AviationWeather.gov Data API | https://aviationweather.gov/api/data/ | response: bare-array (format=json); raw METAR text is the default (no format param)<br>pagination: none<br>filters: ids=comma list<br>keys: icaoId | 2 | 0 | 0 | 2 |
| Bluesky AppView (XRPC) | https://public.api.bsky.app/xrpc/ | response: named-key:feed + cursor<br>pagination: cursor (limit + cursor)<br>deviations: XRPC method names in the URL<br>errors: 400 {"error","message"} | 1 | 0 | 0 | 1 |
| Câmara dos Deputados Dados Abertos | https://dadosabertos.camara.leg.br/api/v2/ | response: dados + links[] ({rel,href} array), for both list and detail<br>pagination: pagina + itens; total in header X-Total-Count<br>filters: field=value, comma lists (siglaUf=SP,RJ)<br>sort: ordem=asc\|desc + ordenarPor=field<br>keys: int id<br>relations: idLegislatura fk, siglaPartido -> partidos?sigla=, nested frentes/{id}/membros<br>errors: 404/400 problem+json<br>rate_limit: Retry-After: 30 on every response; slow (1-10 s) | 15 | 0 | 1 | 2 |
| Cat Facts | https://catfact.ninja/ | response: flat Laravel paginator JSON (data, total, next_page_url, links[])<br>pagination: page + limit; total in body:total; next in body:next_page_url<br>keys: none (fact text only, used as primaryKey) | 5 | 0 | 0 | 2 |
| Bank of Russia daily rates (XML) | https://www.cbr.ru/scripts/ | response: xml (windows-1251)<br>formats: xml only | 0 | 0 | 0 | 1 |
| City of Chicago (Socrata SODA) | https://data.cityofchicago.org/resource/ | response: bare-array<br>pagination: offset ($limit + $offset); no totals (separate $select=count(*) needed)<br>filters: field=value exact; SoQL $where<br>sort: $order=field [DESC] (space-separated, not the dash style default)<br>keys: string id<br>rate_limit: throttled without app token; slow (up to 8 s) | 6 | 0 | 0 | 1 |
| CKAN Action API (demo.ckan.org) | https://demo.ckan.org/api/3/action/ | response: help/success/result envelope; nested:result.results<br>pagination: rows + start; total in result.count<br>filters: fq=field:value (Solr); a bare field=value param 400s<br>sort: sort="field asc\|desc" (space-separated)<br>keys: name/uuid via ?id=<br>deviations: rpc action names in the URL<br>errors: 404/400 {"success":false,"error":{...}} | 4 | 0 | 0 | 3 |
| crates.io | https://crates.io/api/v1/ | response: named-key:crates + meta{total,next_page} / named-key:crate detail<br>pagination: page + per_page<br>total_location: body:meta.total<br>filters: q=, category=, ids[]= (repeated only)<br>sort: sort=downloads\|recent-downloads\|alpha\|new (bare keyword, implied direction)<br>keys: crate name<br>errors: 404 {"errors":[{"detail"}]}<br>rate_limit: crawler policy 1 req/s, UA required | 5 | 0 | 0 | 2 |
| Crossref REST API | https://api.crossref.org/ | response: double envelope: message.items (list) / message (detail), with a status wrapper<br>pagination: rows + offset; total in message.total-results<br>filters: filter=key:value,key:value, a single packed param; unknown top-level params 400<br>sort: sort=field&order=asc\|desc (separate params)<br>keys: DOI (contains slash) for works, int id for members<br>errors: 404 text/plain; 400 JSON validation-failure<br>rate_limit: polite pool via mailto in User-Agent (harness UA already carries a repo URL) | 9 | 0 | 0 | 1 |
| Disney API | https://api.disneyapi.dev/ | response: info + data (array for list, object for detail)<br>pagination: page + pageSize; no total (info.count is the page count, info.totalPages)<br>filters: name= (fuzzy)<br>keys: int _id<br>errors: unknown id -> 200 {"data":[]} | 5 | 0 | 0 | 3 |
| Dog CEO | https://dog.ceo/api/ | response: named-key:message (object map or string list) + status<br>pagination: none<br>keys: breed name as path segment<br>errors: 404 {"status":"error","message","code":404} | 1 | 0 | 0 | 2 |
| Drupal.org JSON:API | https://new.drupal.org/jsonapi/ | response: jsonapi<br>pagination: offset via page[limit] + page[offset]; no meta.count<br>filters: filter[field]=value; exact-match filters are unreliable on a changing dataset<br>sort: sort=-field<br>keys: uuid<br>relations: relationships links<br>errors: jsonapi errors[] (unknown uuid observed timing out) | 5 | 0 | 0 | 3 |
| DummyJSON | https://dummyjson.com/ | response: named-key:<resource> (products/users/posts) + total/skip/limit; detail bare-object<br>pagination: offset via limit+skip, total in body<br>sort: sortBy+order<br>keys: int id<br>relations: fk attribute userId + nested url users/{id}/posts<br>errors: 404 {"message"}; /http/{status} synthetic<br>writes: faked (POST products/add, PUT/PATCH/DELETE products/{id})<br>auth: JWT via POST auth/login, GET auth/me | 21 | 0 | 0 | 2 |
| Europe PMC REST | https://www.ebi.ac.uk/europepmc/webservices/rest/ | response: nested:resultList.result + hitCount + nextCursorMark<br>pagination: cursor (pageSize + cursorMark); total in body:hitCount<br>filters: query= Lucene<br>formats: xml default, format=json | 1 | 0 | 0 | 1 |
| Fake Store API | https://fakestoreapi.com/ | response: bare-array / bare-object<br>pagination: none (limit only)<br>sort: sort=asc\|desc (value only, by id); ignored on other fields<br>keys: int id<br>relations: carts.userId -> users/{id}<br>writes: faked<br>errors: unknown id -> 200 empty body | 9 | 0 | 0 | 3 |
| FBI Wanted | https://api.fbi.gov/ | response: items + total<br>pagination: page + pageSize; total in body:total<br>filters: field=value<br>sort: sort_on + sort_order<br>keys: uid<br>errors: 404 {"detail"} | 7 | 0 | 0 | 0 |
| U.S. Treasury Fiscal Data | https://api.fiscaldata.treasury.gov/services/api/fiscal_service/ | response: jsonapi-style data + meta + links<br>pagination: page[number] + page[size]; total in meta.total-count; next in links.next<br>filters: filter=field:op:value (in:(a,b)), a single packed param<br>sort: sort=-field (JSON:API dash style)<br>keys: none; rows have no id and no detail endpoint<br>errors: 400 JSON for bad params; 404 for any /rates_of_exchange/{x} sub-path | 6 | 0 | 0 | 2 |
| GBIF API | https://api.gbif.org/v1/ | response: results + offset/limit/endOfRecords/count<br>pagination: offset + limit; total in body:count (absent on /children)<br>filters: field=value; no sort parameters<br>keys: int key (species) or uuid key (dataset)<br>relations: parentKey -> species/{key}; species/{key}/children; occurrence/search?speciesKey=<br>errors: 404 JSON; 400 text/plain | 11 | 0 | 0 | 2 |
| Genrenator | https://binaryjazz.us/wp-json/genrenator/v1/ | response: bare JSON string (a quoted scalar, not an object or array) | 0 | 0 | 0 | 1 |
| GitHub REST API (anonymous) | https://api.github.com/ | response: bare-array / bare-object<br>pagination: Link header only (per_page + page/after cursor)<br>errors: 422 {"message","errors":[...]}; 404 JSON<br>rate_limit: 60 req/h core, 10/min search, anonymous — this catalog is capped at 5 requests per live run | 4 | 0 | 0 | 1 |
| GOV.UK Bank Holidays | https://www.gov.uk/ | response: top-level object keyed by division {england-and-wales:{division,events[]}}<br>pagination: none<br>deviations: .json suffix | 2 | 0 | 0 | 0 |
| Countries GraphQL API | https://countries.trevorblades.com/ | response: graphql {data} / {errors}<br>pagination: none<br>deviations: single POST endpoint<br>errors: 200 with errors[] | 1 | 0 | 0 | 1 |
| Hacker News (Firebase) | https://hacker-news.firebaseio.com/v0/ | response: bare int arrays (topstories) / bare-object item<br>pagination: none<br>keys: int id<br>deviations: .json suffix required on every path<br>errors: unknown id -> 200 literal null | 0 | 0 | 0 | 2 |
| httpbin | https://httpbin.org/ | kind: echo / test service<br>response: request echo objects<br>errors: any status via /status/{code}<br>auth: bearer and basic test endpoints | 15 | 0 | 0 | 0 |
| httpbingo | https://httpbingo.org/ | kind: echo / test service (go-httpbin)<br>response: request echo objects; header values are arrays (multi-value)<br>errors: any status via /status/{code}<br>auth: bearer and basic-auth/{user}/{pass} test endpoints | 9 | 0 | 0 | 0 |
| IBGE Localidades | https://servicodados.ibge.gov.br/api/v1/localidades/ | response: bare-array / bare-object (gzip always)<br>pagination: none<br>filters: none (pipe-separated ids in path)<br>sort: orderBy=field (asc only)<br>keys: int id<br>relations: estados/{id}/municipios nested<br>errors: unknown id -> 200 [] | 4 | 0 | 0 | 3 |
| icanhazdadjoke | https://icanhazdadjoke.com/ | response: bare-object (detail) / results + total_jokes (search)<br>pagination: page + limit; total in body:total_jokes<br>content_negotiation: Accept: application/json required (HTML otherwise)<br>errors: 200 with {"status":404}<br>quirks: search result ordering is non-deterministic across pages, so page-based pagination cannot be verified | 2 | 0 | 0 | 2 |
| JSONPlaceholder | https://jsonplaceholder.typicode.com/ | engine: json-server<br>response: bare array / bare object<br>pagination: _page + _limit; total only in X-Total-Count and Link headers<br>filters: field=value; repeated params for OR<br>sort: _sort + _order<br>keys: integer id<br>relations: foreign-key filters and nested URLs<br>writes: faked, not persisted | 19 | 0 | 0 | 2 |
| mail.tm | https://api.mail.tm/ | response: hydra:Collection (hydra:member, hydra:totalItems) — only for Accept: application/ld+json; plain application/json (the harness default) answers a bare array instead<br>pagination: page (hydra:view); next in hydra:view.hydra:next<br>total_location: body:hydra:totalItems<br>keys: id + @id IRI | 1 | 0 | 0 | 2 |
| MyGene.info | https://mygene.info/v3/ | response: Elasticsearch hits + total (query) / bare-object (gene)<br>pagination: offset via from + size<br>filters: q= Lucene syntax only; bare field=value params are ignored<br>keys: _id (entrez string)<br>errors: 404 {"code":404,"success":false} | 6 | 0 | 0 | 1 |
| NHTSA vPIC | https://vpic.nhtsa.dot.gov/api/vehicles/ | response: Count + Message + SearchCriteria + Results<br>pagination: none<br>filters: RPC method + path params<br>keys: MakeId<br>formats: xml default, format=json<br>errors: unknown id -> 200 Count 0 | 2 | 0 | 0 | 1 |
| Nobel Prize API 2.1 | https://api.nobelprize.org/2.1/ | response: named-key:laureates + meta + links; detail is a one-element array<br>pagination: offset + limit; total in body:meta.count; next in body:links.next<br>filters: field=value (gender, nobelPrizeCategory comma)<br>sort: sort=asc\|desc (value only, always by name)<br>keys: string id<br>errors: unknown id -> 200 [{"meta":..}] | 7 | 0 | 0 | 2 |
| npm Registry | https://registry.npmjs.org/ | response: objects[{package,...}] + total (search) / packument bare-object<br>pagination: offset via from + size<br>filters: text= with qualifiers<br>keys: package name (root-level resources: endpoint "" + id)<br>errors: 404 {"error":"Not found"} | 6 | 0 | 0 | 1 |
| OData Northwind v4 | https://services.odata.org/V4/Northwind/Northwind.svc/ | response: value + @odata.context/@odata.count<br>pagination: $top + $skip (+$count=true); total in @odata.count<br>filters: $filter=UnitPrice gt 50<br>sort: $orderby=Field desc (space-separated)<br>keys: Products(1) key predicates, not Products/1<br>errors: 400 JSON for a wrong-shaped path (curl-verified — not the XML the discovery notes assumed) | 4 | 0 | 0 | 1 |
| Open5e | https://api.open5e.com/ | response: results + count/next/previous (DRF)<br>pagination: page + limit<br>filters: django field / field__op (level__gte); django __in only reliable on some fields<br>sort: ordering=-field<br>keys: slug (v1) / key (v2)<br>relations: v1 flat document__slug fk; v2 relations are embedded objects, not fks<br>errors: 404 {"detail"}<br>quirks: DRF APPEND_SLASH: single-item GETs 301-redirect (list endpoints here use a trailing-slash endpoint to avoid it) | 12 | 0 | 0 | 2 |
| OpenAlex | https://api.openalex.org/ | response: results + meta<br>pagination: page + per-page; total in meta.count<br>filters: filter=field:value,field:value (colon pairs, \| for OR)<br>sort: sort=field:asc\|desc (suffix style)<br>keys: full URL ids (https://openalex.org/W...), not the path segment the API accepts<br>rate_limit: anonymous daily budget (observed X-RateLimit-Remaining 984, cost_usd in meta) — keep this catalog's scenario count and attempts minimal | 3 | 0 | 0 | 3 |
| Open Brewery DB | https://api.openbrewerydb.org/v1/ | response: bare-array<br>pagination: page + per_page; total only via a separate /breweries/meta request<br>filters: by_type/by_state/by_city/by_ids (comma)<br>sort: sort=field:asc\|desc (suffix style)<br>keys: uuid<br>errors: 404 HTML | 9 | 0 | 0 | 1 |
| OpenF1 | https://api.openf1.org/v1/ | response: bare-array telemetry with fk filters<br>pagination: none<br>filters: field=value; operators are appended to the field name (date_start>=...), not bracketed; repeated params for OR<br>keys: meeting_key / session_key<br>relations: fk filters only (sessions?meeting_key=...), no path-based detail endpoints<br>errors: empty result -> 404 {"detail":"No results found."}<br>rate_limit: documented 3 req/s, 30 req/min anonymous | 3 | 0 | 0 | 4 |
| openFDA | https://api.fda.gov/ | response: meta + results<br>pagination: limit + skip; total in meta.results.total; next only in the Link header<br>filters: search=field:value (Lucene syntax); a bare field=value param is rejected<br>sort: sort=field:asc\|desc (suffix style)<br>keys: safetyreportid<br>errors: 400 JSON for a bad param; 404 JSON for a route/search with no matches<br>rate_limit: documented 240/min, 1000/day without a key | 3 | 0 | 0 | 3 |
| Open Library Search | https://openlibrary.org/ | response: named-key:docs, Solr-style numFound<br>pagination: limit + offset; total in numFound<br>filters: q plus title=/author= search params<br>sort: sort=new\|old\|rating (fixed keywords, not field+direction)<br>keys: path keys, e.g. "/works/OL27479W" under a key field; ids never appear as a bare "id" attribute | 3 | 0 | 0 | 2 |
| Open-Meteo | https://api.open-meteo.com/v1/ | response: bare-object with parallel arrays (hourly.time[], hourly.temperature_2m[])<br>pagination: none<br>filters: latitude/longitude + variable lists<br>errors: 400 {"error":true,"reason"}<br>rate_limit: documented 10k/day non-commercial | 1 | 0 | 0 | 1 |
| OpenSky Network | https://opensky-network.org/api/ | response: states: array of positional arrays, index 0..16 (icao24, callsign, ...)<br>filters: bbox lamin/lomin/lamax/lomax<br>rate_limit: anonymous credit budget (~400/day)<br>errors: no bbox match -> states:null | 0 | 0 | 0 | 1 |
| OpenVan.camp | https://openvan.camp/api/ | response: named-key:events (list) / bare-object (event/{slug})<br>pagination: page (validated)<br>filters: locale=<br>keys: slug<br>errors: Laravel 422 {"message","errors":{field:[..]}}; 404 JSON<br>quirks: "find event" / "missing event" pin an event ending 2026-09-20; both the events list and the site's own sitemap only ever expose current/upcoming events (curl-verified: no reachable past-event slug), so whether event/{slug} still resolves once this one ends is unconfirmed — refresh the pinned slug after that date. | 4 | 0 | 0 | 0 |
| PokéAPI | https://pokeapi.co/api/v2/ | response: results envelope with count / next / previous<br>pagination: offset + limit, absolute next URL<br>keys: name or integer id in the path<br>filters: none on list endpoints<br>detail: large nested objects | 7 | 0 | 0 | 1 |
| Postcodes.io | https://api.postcodes.io/ | response: status + result (object or array)<br>pagination: none (limit on query)<br>filters: q=<br>keys: postcode / outcode strings<br>errors: 404 {"status":404,"error"} | 4 | 0 | 0 | 2 |
| Postman Echo | https://postman-echo.com/ | kind: echo<br>response: echo {args,data,json,headers,url}<br>errors: /status/{code} -> {"status":code}<br>auth: /basic-auth (postman:password), validated; bearer and header auth are echoed only, not validated<br>writes: verb-specific paths /post /put /patch /delete, not persisted | 15 | 0 | 0 | 1 |
| PotterDB | https://api.potterdb.com/v1/ | response: jsonapi (data[{id,type,attributes,relationships,links}])<br>pagination: page[size] + page[number]; total in meta.pagination.records<br>filters: filter[<field>_<ransack predicate>]; __in needs indexed array params, comma/indexed lists are ignored<br>sort: sort=-field<br>keys: uuid<br>relations: books/{id}/chapters nested; other relations live under relationships.<name>.data, not a flat attribute<br>errors: 404 jsonapi errors[] | 12 | 0 | 0 | 2 |
| Radio Browser | https://de1.api.radio-browser.info/json/ | response: bare-array<br>pagination: offset via offset + limit; no totals<br>filters: search params (countrycode, tag), byuuid?uuids=comma (comma-joined works here)<br>sort: order=field&reverse=true\|false<br>keys: stationuuid | 3 | 0 | 0 | 2 |
| Represent (Open North) | https://represent.opennorth.ca/ | response: objects + meta{offset,limit,total_count,next,previous} (Tastypie-like)<br>pagination: limit + offset; total in meta.total_count<br>filters: field=value, field__istartswith (django-style lookups)<br>sort: none<br>keys: none; resources are identified by url path strings<br>errors: an unrecognised sub-path answers 200 with empty objects, not 404 | 6 | 0 | 0 | 2 |
| ReqRes | https://reqres.in/api/ | response: data (list and detail) + support/_meta noise<br>pagination: page + per_page, total in body<br>keys: int id<br>errors: 404 {}; 400 {"error"}; 403 invalid_api_key<br>writes: faked, echoes body (flat, no envelope on update/patch)<br>auth: x-api-key header (public free key reqres-free-v1; wrong key -> 403, no key -> 200) | 14 | 0 | 0 | 2 |
| restful-api.dev | https://api.restful-api.dev/ | response: bare-array / bare-object<br>pagination: none (13 seed objects)<br>filters: id=a,b (comma) or repeated id; no generic field filter<br>keys: string id (seed "1".."13", created hex)<br>writes: persisted; seed ids reserved (405)<br>errors: 404 {"error"}, 405 reserved id<br>quota: 50 requests/day per IP; exhaustion answers HTTP 405 with a rate-limit body | 8 | 0 | 0 | 1 |
| Rick and Morty API | https://rickandmortyapi.com/api/ | response: results + info{count,pages,next,prev}<br>pagination: page (fixed page size 20); total in info.count<br>filters: field=value<br>sort: none<br>keys: int id<br>relations: absolute URL strings (origin.url, episode[]), no fk ids<br>errors: 404 {"error"} | 9 | 0 | 0 | 3 |
| Scryfall | https://api.scryfall.com/ | response: object:list data + has_more + total_cards; detail bare object<br>pagination: page (fixed 175 per page)<br>filters: q= search DSL only; bare field=value params are ignored<br>sort: order=field&dir=asc\|desc<br>keys: set code / uuid<br>headers: Accept and User-Agent must be present with any value (400 only when a header is fully absent, curl-verified); the harness always sends both by default<br>errors: 404/400 {"object":"error","status","details"}<br>rate_limit: documented ~10 req/s | 8 | 0 | 0 | 2 |
| Solana JSON-RPC (mainnet-beta) | https://api.mainnet-beta.solana.com/ | response: JSON-RPC 2.0 {jsonrpc,id,result\|error}<br>deviations: all-POST<br>errors: 200 with error object<br>rate_limit: public endpoint rate-limited; one request per run | 0 | 0 | 0 | 1 |
| Spaceflight News API | https://api.spaceflightnewsapi.net/v4/ | response: results + count/next/previous (DRF)<br>pagination: limit + offset<br>filters: news_site comma list on the bare field name (not a django __in lookup); range filters are separate named params (published_at_gte)<br>sort: ordering=-published_at (restricted choices)<br>keys: int id<br>errors: 404 {"detail"}; 400 {"ordering":[...]}<br>quirks: DRF APPEND_SLASH: single-item GETs 301-redirect (list endpoints here use a trailing-slash endpoint to avoid it) | 12 | 0 | 0 | 2 |
| data.police.uk | https://data.police.uk/api/ | response: bare-array / bare-object<br>pagination: none (reference endpoints return everything)<br>keys: slug id<br>relations: forces/{id}/people nested<br>errors: 404 text/plain<br>rate_limit: documented 15 req/s | 7 | 0 | 0 | 2 |
| USGS Earthquake Catalog (FDSN) | https://earthquake.usgs.gov/fdsnws/event/1/ | response: geojson FeatureCollection (features[].properties)<br>pagination: offset (1-based); limit+offset<br>filters: named range params (minmagnitude, starttime)<br>sort: orderby=time\|time-asc\|magnitude\|magnitude-asc<br>formats: geojson, csv, xml, kml, text via format=<br>errors: 400 text/plain | 2 | 0 | 0 | 3 |
| Wikipedia Action API | https://en.wikipedia.org/w/ | response: nested:query.<module> (arrays) or query.pages keyed by pageid<br>pagination: continuation-token (continue object echoed back)<br>filters: module params<br>deviations: single api.php, action=, format=json<br>errors: 200 with error / missing markers | 1 | 0 | 0 | 3 |
| Quotes on Design (WordPress REST API) | https://quotesondesign.com/wp-json/wp/v2/ | response: bare-array<br>pagination: page + per_page<br>total_location: header:X-WP-Total (+X-WP-TotalPages); next only in header:Link<br>filters: author=, categories=, include=<br>sort: orderby+order<br>keys: int id<br>errors: 404/400 {"code","message","data":{"status"}} | 8 | 0 | 0 | 2 |
| World Bank Indicators API | https://api.worldbank.org/v2/ | response: positional [meta, rows] tuple; total in meta.total (index 0)<br>pagination: page + per_page<br>sort: none<br>keys: string id (ISO3)<br>formats: XML by default; every request needs format=json (every scenario here sets it explicitly)<br>errors: 200 with a [{"message":[...]}] error body instead of 404 | 4 | 0 | 0 | 1 |

## Failures

_None._

## Limitations

- **Art Institute of Chicago API › search filters and sort** — Field filters and sorting exist only on /artworks/search as Elasticsearch params (query[term][field]=, sort[field][order]=); a plain field=value filter on /artworks is silently ignored (title=Starry still returns the default unfiltered page). Workaround: from('artworks/search')->withQuery([...]). (r…
- **Art Institute of Chicago API › batched artists** — Membership param is ids while the primary key is id; BelongsTo batch sends whereIn(<primary key name>) as id=a,b, which artic ignores (returns the default unfiltered page), so no related agent resolves. (reproduced: Failed asserting that 0 is greater than 0.)
- **AviationWeather.gov Data API › default format is raw text** — Without format=json the response is a raw METAR text line, not JSON, so decoding it fails outright. (reproduced: Unable to decode response JSON: Syntax error)
- **AviationWeather.gov Data API › no detail endpoint or pagination** — There is no path-based detail route: GET metar/KJFK 404s ("Not found") even though the station exists in the ids= list form — that is what the attempt reproduces. paginate.lazy is a body-shape fact rather than an exercised call: the response is a bare array with no total, next link or page parameter…
- **Bluesky AppView (XRPC) › cursor paging has no total or offset** — The next page is reachable only via an opaque cursor token returned in the body; the whole envelope is curl-verified {feed, cursor} — no count, no page number, no offset. The attempt configures pagination.total at a plain "total" key to show the response has nothing to hold it: paginate() fetches th…
- **Câmara dos Deputados Dados Abertos › paginate with total** — Total is only exposed in the X-Total-Count header (and next only via the Link header or a positional links[] array); paginate() reads totals from the response body. (reproduced: Pagination total key is not configured for [Sanchescom\Rest\Tests\Live\Catalog\Camara\Deputado]; set 'pagination.total' or…
- **Câmara dos Deputados Dados Abertos › retry-after** — No synthetic status endpoint exists here to safely demonstrate a live retry, so client.retry is left absent rather than configured for a feature not actually exercised. (Every response also carries Retry-After: 30, which is why a retry config, if one were added, would need respect_retry_after=false …
- **Cat Facts › detail** — Facts have no ids or a detail endpoint to fetch a single one by key.
- **Cat Facts › laravel preset** — The raw paginator is flat (total, next_page_url), not the 'laravel' preset's meta.total/links.next shape; the preset would read null and paginate() would report a total of 0. Configuring total/next explicitly (as this catalog does) works instead. (reproduced: Pagination total key [meta.total] is mis…
- **Bank of Russia daily rates (XML) › xml only** — The response is windows-1251 XML (curl-verified content-type application/xml; charset=windows-1251), not JSON; the package only decodes JSON bodies, so this needs a custom ClientInterface converting XML to arrays, which is outside a catalog file's scope. (reproduced: Unable to decode response JSON: …
- **City of Chicago (Socrata SODA) › paginate with total** — List responses carry no total; getting a count needs a separate $select=count(*) request that paginate() never issues.
- **CKAN Action API (demo.ckan.org) › rpc detail** — Detail is package_show?id=<name> (an action name plus a query param), not a path segment; find() always appends the id as a path segment (package_show/<name>), which 404s with an HTML body even for a dataset that really exists. (reproduced: REST request to [package_show/my-sample-dataset-001] failed…
- **CKAN Action API (demo.ckan.org) › plain field filter is rejected** — A bare field=value param is not silently ignored but actively rejected: package_search?license_id=uk-ogl answers HTTP 400 "Invalid search parameters: ['license_id']" — the default grammar has no other way to render a where() clause. (reproduced: REST request to [package_search] failed with status 40…
- **CKAN Action API (demo.ckan.org) › writes** — package_create is a POST-only RPC action requiring an API token; the public demo instance is not an anonymous write sandbox.
- **crates.io › bracket membership** — ids[]=serde&ids[]=rand (repeated) works, curl-verified 2 crates; the package's whereIn renders one comma-joined param (ids[]=serde,rand) instead, which curl-verified returns 0 crates. (reproduced: Failed asserting that 0 is equal to 2 or is greater than 2.)
- **crates.io › keyword sort** — sort takes a bare keyword with an implied direction (sort=downloads, curl-verified: properly desc by downloads); orderBy() renders sort=-downloads for desc, which curl-verified is ignored (falls back to the default order, not sorted by downloads at all). (reproduced: Failed asserting that two arrays…
- **Crossref REST API › filters** — Filters pack field:value pairs into one param (filter=member:78,type:journal-article); a plain where() renders member=78 as a bare top-level param, which the API rejects outright (curl-verified: 400 unknown-parameter) rather than filtering. (reproduced: REST request to [works] failed with status 400…
- **Disney API › paginate with total** — info.count is the number of items on the current page, not the collection total (only info.totalPages is available). (reproduced: Failed asserting that 50 is greater than 50.)
- **Disney API › missing character** — Unknown id returns HTTP 200 {"info":{"count":0},"data":[]} instead of a 404, so ModelNotFoundException never fires. (reproduced: Expected HTTP 404, but the request succeeded.)
- **Disney API › name filter** — name= is a fuzzy match, not exact equality: name=Mickey Mouse also returns "Lion (Mickey Mouse Works)", so filtered results are not all equal to the requested value. (reproduced: Failed asserting that two strings are identical.)
- **Dog CEO › breed map loses key names** — breeds/list/all answers a top-level object keyed by breed ({"affenpinscher":[],"african":["wild"],...}, curl-verified); hydrate() strips the breed names via array_values() and hands each sub-breed value to Model::fill(). A JSON list decodes to plain int keys 0..n, and fill() passes those straight to…
- **Dog CEO › sub-breed string lists hydrate empty** — breed/{name}/list answers a bare array of strings (["afghan","basset",...]); hydrate() only keeps array items, so every string becomes an attribute-less model. The same shape would break a nested hasMany relation onto this endpoint. (reproduced: Expecting null not to be null .)
- **Drupal.org JSON:API › paginate with total** — Drupal omits meta.count by default, so paginate() has no total to read. (reproduced: Pagination total key is not configured for [Sanchescom\Rest\Tests\Live\Catalog\DrupalJsonapi\ProjectModule]; set 'pagination.total' or use simplePaginate().)
- **Drupal.org JSON:API › missing module** — An unknown uuid request does not respond (times out after 20s+) instead of returning 404; not reliable enough for a probe, and an attempt here would hang every live run.
- **Drupal.org JSON:API › lazy walk modules** — Every page silently omits some rows the current session can't view (meta.omitted 'insufficient authorization'), so a page[limit]=10 request returns fewer than 10 items (observed 5, 9, 8); lazy()'s exact-request-count assumption (a full page until the last one) never holds on this endpoint. (reproduc…
- **DummyJSON › field filters** — No generic field=value filter: posts?userId=1 is ignored (returns all 251 posts). Only path filters exist (/products/category/{c}, /users/filter?key=&value=). (reproduced: Failed asserting that two strings are identical.)
- **DummyJSON › has-many by fk** — fk filter ignored (posts?userId={id} returns all posts); use the nested users/{id}/posts relation instead. (reproduced: Failed asserting that two strings are identical.)
- **Europe PMC REST › cursorMark paging** — hitCount is a readable body total, but the next page is addressed only via the opaque nextCursorMark token from the previous response, not a page number or offset — curl-verified: page=1 and page=2 (or the page-name paginate() sends) answer the exact same two result ids with hitCount unchanged, so p…
- **Fake Store API › sort** — sort=asc|desc carries only a direction and always orders by id; none of the package's sort styles render a bare direction for an arbitrary field. Workaround: withQuery(['sort' => 'desc']). (reproduced: Failed asserting that two arrays are identical.)
- **Fake Store API › missing product** — Unknown id returns HTTP 200 with an empty body, so no ModelNotFoundException is thrown. (reproduced: Expected HTTP 404, but the request succeeded.)
- **Fake Store API › pagination** — Only a limit parameter exists; page/offset params are silently ignored (the default page style applies but the API ignores page) and there is no total in the response. (reproduced: Pagination total key is not configured for [Sanchescom\Rest\Tests\Live\Catalog\Fakestore\Product]; set 'pagination.tota…
- **U.S. Treasury Fiscal Data › filters** — Filters use a single packed param filter=field:op:value (field:in:(a,b)); a plain where() renders filter[country]=France, which the API silently ignores (curl-verified: still returns the unfiltered, unrelated default page instead of France-only rows). (reproduced: Failed asserting that two strings a…
- **U.S. Treasury Fiscal Data › row detail** — Rows have no id and no detail endpoint; any /rates_of_exchange/{x} sub-path 404s regardless of the value given. (reproduced: REST request to [v1/accounting/od/rates_of_exchange/1] failed with status 404.)
- **GBIF API › membership** — Membership needs repeated params for OR (rank=GENUS&rank=FAMILY works); a comma list is rejected outright for an enum field like rank (400 "Cannot parse GENUS,FAMILY into a known Rank"). dataset?key= has no filter at all (key=A, key=A&key=B, key=A,B all return the full corpus), so that endpoint cann…
- **GBIF API › sorting** — No sort parameters on any list endpoint.
- **Genrenator › scalar body** — The body is a bare JSON string literal (curl-verified: "seattle panpipe reel", content-type application/json) — valid JSON but not an object or array; Json::decode() requires the decoded value to be an array and throws otherwise, so hydration never runs. (reproduced: Response JSON must decode to an …
- **GitHub REST API (anonymous) › link header total** — No pagination.total is configured here, so paginate() has no body path to a total; the true count lives only in the Link header's rel="last" page number. No attempt: verifying it would spend more of the anonymous rate-limit budget than this catalog's 5-request ceiling allows. paginate.simple and pag…
- **Countries GraphQL API › graphql only** — Every read is a POST {query} to one endpoint; find()/list() always issue a GET, which the server answers with an empty 204 body, so hydration always yields an attribute-less model — GraphQL query errors also come back as HTTP 200 with errors[], not a REST-style status code. (reproduced: Failed asser…
- **Hacker News (Firebase) › suffix and null id** — Items live at item/{id}.json — a .json suffix appended after the id, not a bare path segment; find() renders item/8863 (no suffix), which 301-redirects (curl-verified Location: console.firebase.google.com/...) to the Firebase console's HTML UI, and decoding that HTML fails outright — that redirect-a…
- **Hacker News (Firebase) › id lists hydrate empty** — topstories.json (and every kids[] list) is a bare array of integers, not objects; hydrate() only keeps array items as attributes, so each bare int becomes an attribute-less model — the same shape would break a nested hasMany relation onto kids. (reproduced: Expecting null not to be null .)
- **IBGE Localidades › missing state** — Unknown id returns HTTP 200 with [] instead of a 404, so ModelNotFoundException never fires. (reproduced: Expected HTTP 404, but the request succeeded.)
- **IBGE Localidades › sort** — The default grammar sends sort=-nome (not the orderBy=nome the API actually reads), so the API silently ignores the param entirely and returns its fixed default order regardless of requested field or direction (curl-verified: sort=nome and sort=-nome both return the same order). (reproduced: Failed …
- **IBGE Localidades › multi ids** — Multiple ids are pipe-separated path segments (estados/33|35), not a query parameter; whereIn() renders id=33,35 as a query string, which the API ignores. Workaround: from('estados/33|35'). (reproduced: Failed asserting that two arrays are identical.)
- **icanhazdadjoke › missing joke** — Unknown id answers HTTP 200 with {"message":..,"status":404} instead of a 404 status, so ModelNotFoundException never fires. (reproduced: Expected HTTP 404, but the request succeeded.)
- **icanhazdadjoke › next page** — next_page is an integer, not a link or has-more flag; the simple-paginate config needs a non-empty string path to a next-page indicator, so only full-page inference is usable. (reproduced: Failed asserting that false is true.)
- **JSONPlaceholder › paginate with total** — Total is only exposed in the X-Total-Count header; paginate() reads totals from the response body.
- **JSONPlaceholder › membership filters** — json-server expects repeated params (id=1&id=2); whereIn renders a comma list or an indexed array (id[0]=1).
- **mail.tm › hydra paging has no total to test** — hydra:totalItems is a perfectly usable pagination.total (a colon-bearing key holds no dots, so Arr::get resolves it), but this instance publishes exactly one domain — curl-verified hydra:totalItems 1, and page=2 answers an empty hydra:member — so paginate()/simplePaginate() have no second page to wa…
- **mail.tm › writes create a real mailbox** — POST /accounts creates a real, permanent temporary mailbox on a public instance — not a sandbox — so it is not exercised here.
- **MyGene.info › filters** — Filtering only understands the Lucene q= syntax; a plain where() renders symbol=CDK2 as a bare top-level param, which mygene ignores entirely (curl-verified: still matches the whole index, ~93M hits, not just CDK2). Batch lookup by many ids is a POST-only endpoint, which the package cannot use for r…
- **NHTSA vPIC › rpc paths** — The method name and id share one URL segment (GetModelsForMakeId/{id}) and the payload wraps a *list* of models under Results, not one object, so find() hydrates the whole list as a single model's attributes (getKey() then misses). The attempt covers the error half: an unknown id answers HTTP 200 wi…
- **Nobel Prize API 2.1 › sort** — sort=asc|desc carries only a direction, always ordering by name; there is no field parameter, so orderBy() on another field cannot change the order (curl-verified: sort=asc and sort=desc both order by knownName regardless of requested field). (reproduced: Failed asserting that two arrays are identic…
- **Nobel Prize API 2.1 › missing laureate** — Unknown id returns HTTP 200 with a meta-only single-element array (no laureate, no id) instead of a 404, so ModelNotFoundException never fires. (reproduced: Expected HTTP 404, but the request succeeded.)
- **npm Registry › nested item keys** — Search result items carry their identity under a nested package.name; the item's own top-level keys are only package, score, searchScore, flags — there is no flat "name" attribute, so getKey() is null for every hit and lazy() cannot dedupe pages by key. (reproduced: Failed asserting that 1 is identi…
- **OData Northwind v4 › key predicate detail** — Detail addressing is a key predicate, Products(1), not a path segment; find() always renders Products/1, which curl-verified answers HTTP 400 JSON ("segment refers to a collection ... must be the last segment") even for an id that exists, so a real product can never be found this way. (The proper Pr…
- **Open5e › silently ignored lookups** — __in is honoured only on some fields: v2 spells level__in and v1 documents slug__in are silently ignored, returning the unfiltered collection instead of an error, so a whereIn can look successful while filtering nothing. (reproduced: Failed asserting that two arrays are identical.)
- **Open5e › v1 document detail** — v1/documents/{slug} returns 404 (only the collection endpoint works), so has-many/belongs-to probes, which fetch the parent by key first, cannot run on v1; v2 relations are embedded objects, not flat fks.
- **OpenAlex › filters** — Filters pack colon pairs into one param (filter=type:article,publication_year:2020, | for OR); the package has no grammar that renders this shape. No attempt here to conserve the anonymous daily request budget.
- **OpenAlex › find by id** — The id returned in results is a full URL (https://openalex.org/W2741809807) while the detail path takes the bare id (W2741809807); getKey() never equals the id used to request the model, and fk values are URLs too. No attempt here to conserve the anonymous daily request budget.
- **OpenAlex › cursor paging** — Beyond 10k results, page-based paging stops working and only cursor=* tokens are accepted; lazy() has no cursor mode. No attempt here to conserve the anonymous daily request budget.
- **Open Brewery DB › paginate with total** — Totals are only available from a separate GET breweries/meta request; list responses carry no total.
- **OpenF1 › empty result is 404** — A filter with no matches answers HTTP 404 (curl-verified: sessions?meeting_key=1 -> 404 "No results found"), so get() throws ModelNotFoundException instead of returning an empty collection. (reproduced: REST request to [sessions] failed with status 404.)
- **OpenF1 › membership** — OR needs repeated params (meeting_key=1228&meeting_key=1229, curl-verified: 8 sessions); the package's whereIn renders one comma-joined param instead (meeting_key=1228,1229), which curl-verified returns 404 "No results found". (reproduced: REST request to [sessions] failed with status 404.)
- **OpenF1 › detail and belongs-to** — There are no path-based detail endpoints; GET meetings/1229 answers 400 (curl-verified), so a single parent cannot be fetched by key, which read.find, belongsTo and the single-parent hasMany probe all require. (reproduced: REST request to [meetings/1229] failed with status 400.)
- **OpenF1 › comparison operators** — Operators are appended directly to the field name (date_start>=2024-03-01, curl-verified: 3 sessions); the plain grammar renders the bracketed form date_start[>=]=2024-03-01 instead, which curl-verified returns 404 "No results found". Workaround: withQuery(). (reproduced: REST request to [sessions] …
- **openFDA › search filters** — Filters are a single Lucene search param (search=patient.drug.medicinalproduct:"ASPIRIN"); a plain where() renders the field name as its own top-level param, which the API rejects outright. (reproduced: REST request to [drug/event.json] failed with status 400.)
- **openFDA › detail and not-found** — There is no path-based detail endpoint (only the list endpoint exists); an empty search answers 404 {"error":{"code":"NOT_FOUND"}} instead of a per-id 404, so read.find has nothing to call. (reproduced: REST request to [drug/event.json/doesnotexist123] failed with status 404.)
- **openFDA › deep paging** — skip is capped at 25000 and the next page is only exposed via the Link response header, which lazy() does not read. No attempt here: reproducing the cap would need thousands of requests against a daily quota.
- **Open Library Search › entity detail** — With the Accept: application/json header the harness always sends, GET works/OL27479W answers 200 JSON directly (curl-verified, no redirect); the attempt fails because the entity's id is the path string "/works/OL27479W" under a key field, which never appears as a bare id attribute, so read.find's k…
- **Open Library Search › sort** — sort takes fixed keywords (new, old, rating), not field + direction; orderBy() renders sort=title (ignored, curl-verified) and sort=-title (curl-verified: 500 Internal Server Error). (reproduced: REST request to [search.json] failed with status 500.)
- **Open-Meteo › parallel arrays** — Time series come back as column-oriented parallel arrays (hourly.time[] and hourly.temperature_2m[] zipped by index), not row objects; pointing dataKey at the value column hydrates each bare float into an attribute-less model instead of a per-hour row. (reproduced: Expecting null not to be null .)
- **OpenSky Network › positional rows crash hydration** — Each state is a positional array with no field names ([icao24, callsign, origin_country, ...], curl-verified); json_decode gives it plain int keys 0..16, and Model::fill() passes those straight to isFillable(string $key) — which has a strict string parameter — so hydrating even one row throws a Type…
- **PokéAPI › list filters** — PokéAPI list endpoints accept only offset and limit.
- **Postcodes.io › bulk lookup** — Bulk lookup is a POST {"postcodes":[...]} read (returns a 200 with per-postcode results); the package treats any POST as a write, so there is no whereIn() mapping to it.
- **Postcodes.io › postcode keys** — find() itself works (see "find outcode"); this is a narrower key-normalisation gap: postcodes/SW1A2AA (no space) still resolves 200 but the API re-inserts the space in the response ("SW1A 2AA"), so getKey() never equals the squished id that was requested for this specific input format. (reproduced: …
- **Postman Echo › resource reads** — Not a resource API; there is nothing to list, paginate or filter.
- **PotterDB › membership** — slug_in is ignored in every spelling (comma, [], [0]/[1]). (reproduced: Failed asserting that two arrays are identical.)
- **PotterDB › belongs-to** — Related ids live under relationships.<name>.data.id (or as an array of {id,type} for to-many), not a flat top-level attribute; belongsTo reads a flat foreign key attribute on the parent.
- **Radio Browser › boolean direction** — Direction is reverse=true|false; orderBy() renders sort=-votes (curl-verified: results not ordered by votes at all, since "sort" isn't a recognised param here), while order=votes&reverse=true curl-verified sorts correctly. Workaround: withQuery(['order'=>'votes','reverse'=>'true']). (reproduced: Fai…
- **Radio Browser › paginate with total** — No total count is returned anywhere in the response body. (reproduced: Pagination total key is not configured for [Sanchescom\Rest\Tests\Live\Catalog\RadioBrowser\Station]; set 'pagination.total' or use simplePaginate().)
- **Represent (Open North) › membership** — elected_office__in=MP,MPP is silently ignored (unfiltered results returned). (reproduced: Failed asserting that two arrays are identical.)
- **Represent (Open North) › ids** — Resources are identified by url path strings (/boundary-sets/federal-electoral-districts/) with no id attribute. get(id) requests representatives/{id}, which 301-redirects (APPEND_SLASH) to representatives/{id}/ and answers 200 {"objects":[],"meta":{"total_count":0}} — a valid but keyless empty resp…
- **ReqRes › filters and sort** — No query filters or sorting on any endpoint.
- **ReqRes › relations** — No related resources exposed by the API.
- **restful-api.dev › pagination/sort/filters** — Whole collection is returned in one response; page params are ignored; only an id-membership filter exists (no generic field=value filter). (reproduced: Failed asserting that two strings are identical.)
- **Rick and Morty API › url relations** — Relations are absolute URL strings (origin.url, location.url, episode[]) with no fk id attribute for belongsTo/hasMany to read.
- **Rick and Morty API › sort** — No sort parameter; sort=name is silently ignored (curl-verified: same unsorted, unfiltered order regardless of direction). (reproduced: Failed asserting that two arrays are identical.)
- **Rick and Morty API › multi-get** — Batch fetch is a path list (character/1,2,3), not a query param; id=1,2,3 is silently ignored (curl-verified: full unfiltered page returned). (reproduced: Failed asserting that two arrays are identical.)
- **Scryfall › page size** — Page size is fixed at 175 (~400 KB per page) and the limit param is not accepted, so a lazy() walk would pull multi-hundred-KB pages just to prove it; no attempt here to avoid that cost. paginate.total is not blocked by this — see "paginate by total_cards" above.
- **Scryfall › filters** — All filtering lives in the q DSL (set:khm type:god); a plain where() renders rarity=mythic as a bare top-level param, which Scryfall ignores outright (curl-verified: still 323 cards, mixed rarities returned) rather than filtering. (reproduced: Failed asserting that two strings are identical.)
- **Solana JSON-RPC (mainnet-beta) › json-rpc** — Every call is POST {"jsonrpc":"2.0",...} to one endpoint; find()/list() always issue a GET, which the server rejects with HTTP 405 "Bad method" (curl-verified {"jsonrpc":"2.0","error":{"code":405,"message":"Bad method"}}), so no read ever reaches a result. The package raises a correct typed client e…
- **Spaceflight News API › range filters** — Range filters are separate named params (published_at_gte) rather than field__op or field[op]. Workaround: withQuery(['published_at_gte' => '2026-01-01']).
- **Spaceflight News API › relations** — launches/events are embedded arrays of {launch_id, provider}; there is no fk attribute to a sibling resource.
- **data.police.uk › paging/sort** — Reference endpoints (forces, forces/{id}/people) return the whole collection with no page/sort params; crime endpoints filter by lat/lng/date (see "bicycle theft filter") but still return everything for that month with no way to sort or page the result.
- **data.police.uk › eager nested people** — Not a package limitation: the parent forces list cannot be limited (44 forces), so eager-loading people for all of them would fire 44 concurrent requests just to prove a mechanism already covered by the nested has-many probe. Skipped as a politeness skip toward the API, not because eager loading fai…
- **USGS Earthquake Catalog (FDSN) › one-based offset** — offset must be >= 1; the offset pagination style always sends offset=0 on the first page, which the API rejects (curl-verified 400 for offset=0, valid from offset=1). (reproduced: REST request to [query] failed with status 400.)
- **USGS Earthquake Catalog (FDSN) › csv option is not decodable** — format=csv answers text/csv from the same endpoint (curl-verified content-type); only format=geojson is JSON-decodable. (reproduced: Unable to decode response JSON: Syntax error)
- **USGS Earthquake Catalog (FDSN) › sort direction in value** — Direction is encoded in the orderby value itself (orderby=magnitude for descending, orderby=magnitude-asc for ascending). orderBy('magnitude', ...) renders the plain grammar's single sort= parameter, and FDSN has no sort parameter at all: curl-verified HTTP 400 "Unknown parameter \"sort\"." for sort…
- **Wikipedia Action API › continuation pagination** — The next page is reached only by echoing back the "continue" object from the previous response (apcontinue=...&continue=-||). page= and limit= are curl-verified "Unrecognized parameters", so every page the package asks for is the same page: page=1 and page=2 answer the identical pageids, which is wh…
- **Wikipedia Action API › missing page has no error marker** — A missing title answers HTTP 200 with query.pages["-1"].missing set, not a 404 — read.list happily hydrates one model that lacks every normal page field instead of throwing. (reproduced: Expecting null not to be null .)
- **Wikipedia Action API › no resource paths** — Everything goes through api.php?action=...; find() appends the id as a path segment (api.php/<title>), which MediaWiki 301-redirects to bare api.php dropping the query string, landing on the HTML API help page instead of JSON. (reproduced: Unable to decode response JSON: Syntax error)
- **Quotes on Design (WordPress REST API) › paginate with total** — Totals only exist in the X-WP-Total / X-WP-TotalPages response headers, not the body; paginate() has no body path to read a total from. (reproduced: Pagination total key is not configured for [Sanchescom\Rest\Tests\Live\Catalog\WordpressQuotesondesign\Post]; set 'pagination.total' or use simplePagin…
- **Quotes on Design (WordPress REST API) › past last page** — Requesting a page beyond the last returns HTTP 400 rest_post_invalid_page_number instead of an empty array (curl-verified). lazy() has no total/has_more signal configured for this API, so it falls back to full-page inference (count>=perPage); pinning to a fixed 2-post set (include=2568,2563) with a …
- **World Bank Indicators API › missing country** — An unknown id answers HTTP 200 with [{"message":[{"key":"Invalid value"}]}] instead of a 404, so ModelNotFoundException never fires. (reproduced: Expected HTTP 404, but the request succeeded.)

## Skipped (API unavailable during the run)

- **Câmara dos Deputados Dados Abertos › where-in states** — GuzzleHttp\Exception\ConnectException: cURL error 28: Operation timed out after 229441 milliseconds with 0 bytes received (see https://curl.se/libcurl/c/libcurl-errors.html) for https://dadosabertos.camara.leg.br/api/v2/deputados?siglaUf=SP%2CRJ&itens=20

## Not run

_None._
